package com.mycompany.excersize;

import javafx.application.Application;
import javafx.event.ActionEvent;
import javafx.event.EventHandler;
import javafx.geometry.Pos;
import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.scene.layout.HBox;
import javafx.scene.layout.StackPane;
import javafx.scene.layout.VBox;
import javafx.scene.paint.Color;
import javafx.scene.text.Font;
import javafx.scene.text.Text;
import javafx.stage.Stage;
import javafx.scene.control.TextField;
import javafx.scene.image.Image;
 import javafx.scene.image.ImageView;

/**
 *
 * @author zulfa
 */
public class menu extends Application {
    Avatar avatar1 = new Avatar("Rename Me!");
    Avatar avatar2 = new Avatar("Rename Me!");
    Avatar avatar3 = new Avatar("Rename Me!");
    private String avatar1putname = "";
    private Label name1Label = new Label("Avatar 1");
    private ImageView bodyImgView;
    Button editAvatar1;
    TextField avatar1text;
    Button setName;
    int setNameLoop = 0;


    Avatar currentAvatar = null;
    Label avatarLabel; // Label for displaying clothing text
    
    
    @Override
    public void start(Stage primaryStage) {
        // Menu Scene
            Text title = new Text("Play Dress Up!");
            title.setFont(new Font(30));
            title.setFill(Color.CORAL);

            Button play = new Button("Play!");

            VBox main = new VBox(50);
            main.setAlignment(Pos.CENTER);
            main.getChildren().addAll(title, play);

            Scene menuScene = new Scene(main, 500, 300);
            
        // Closet Scene
        Text closetText = new Text("Select An Avatar to Edit!");
        

            // Avatar selection buttons
                Text avatar1Name = new Text(avatar1.getName());
                Button editAvatar1 = new Button("Avatar 1");
                
                
                //setting name
                TextField avatar1text = new TextField();
                Button setName = new Button("Edit Name");
                System.out.println(avatar1.getName());

                
                HBox avatar1box = new HBox(20, avatar1Name, editAvatar1, avatar1text, setName) {{setAlignment(Pos.CENTER);}};

                

                    setName.setOnAction(eo ->
                    {
                        setNameLoop++;
                        if(setNameLoop%2 == 0){
                            avatar1box.getChildren().removeAll(setName);
                            avatar1box.getChildren().addAll(avatar1text, setName);
                            avatar1box.getChildren().removeAll(name1Label);
                            System.out.println(avatar1.getName());

                        }else{
                            avatar1box.getChildren().removeAll(avatar1text, editAvatar1, setName);
                            avatar1box.getChildren().addAll(editAvatar1, setName);
                            avatar1putname = avatar1text.getText();
                            avatar1Name.setText(avatar1putname);
                            avatar1.setName(avatar1putname);
                            System.out.println(avatar1.getName());
                        }
                        
                    });
                
                Text avatar2Name = new Text(avatar2.getName());
                Button editAvatar2 = new Button("Avatar 2");

                Text avatar3Name = new Text(avatar3.getName());
                Button editAvatar3 = new Button("Avatar 3");

                Button back = new Button("Go Back");
                                      
            //vbox            
                VBox avatarSelection = new VBox(50, 
                closetText, avatar1box,
                new HBox(20, avatar2Name, editAvatar2) {{setAlignment(Pos.CENTER);}},
                new HBox(20, avatar3Name, editAvatar3) {{setAlignment(Pos.CENTER);}}, back);
                avatarSelection.setAlignment(Pos.CENTER);
            //scene
                Scene closet = new Scene(avatarSelection, 500, 500);
                play.setOnAction(er -> primaryStage.setScene(closet));
                back.setOnAction(er -> primaryStage.setScene(menuScene));
    
    // Avatar Editing Scene
        
        /*Image img = new Image(("/pics/body.png"));
 
         if (img.isError()) {
             System.out.println("Error loading image!");
         } else {
             System.out.println("Image loaded successfully!");
         }
 
         ImageView imgView = new ImageView(img);
         imgView.setFitWidth(200);
         imgView.setFitHeight(300);*/
        
        
        EventHandler<ActionEvent> avatarSelectionHandler = e -> {

            //new stuff
            ImageView genMale = new ImageView(new Image("/pics/boyIcon.png"));
            genMale.setFitWidth(30);
            genMale.setFitHeight(30);
            
            ImageView genfemale = new ImageView(new Image("/pics/girlIcon.png"));
            genfemale.setFitWidth(30);
            genfemale.setFitHeight(30);
            
            ImageView genNeut = new ImageView(new Image("/pics/nuetIcon.png"));
            genNeut.setFitWidth(30);
            genNeut.setFitHeight(30);
            
            ImageView light = new ImageView(new Image("/pics/light.png"));
            light.setFitWidth(30);
            light.setFitHeight(30);
            
            ImageView med = new ImageView(new Image("/pics/medium.png"));
            med.setFitWidth(30);
            med.setFitHeight(30);
            
            ImageView dark = new ImageView(new Image("/pics/dark.png"));
            dark.setFitWidth(30);
            dark.setFitHeight(30);
            
            HBox genderLayout = new HBox(10, genMale, genfemale, genNeut);
            genderLayout.setAlignment(Pos.CENTER);
            
            HBox skinLayout = new HBox(10, light, med, dark);
            skinLayout.setAlignment(Pos.CENTER);
            //end of new
            
            ImageView shirtImg = new ImageView();
            shirtImg.setFitWidth(215);
        shirtImg.setFitHeight(340);
            ImageView pantImg = new ImageView();
            pantImg.setFitWidth(215);
        pantImg.setFitHeight(340);
            ImageView hatImg = new ImageView();
            hatImg.setFitWidth(215);
        hatImg.setFitHeight(340);
        
        
        avatarLabel = new Label("Select an avatar to start editing!");

        ImageView bodyImgView = new ImageView();
        bodyImgView.setFitWidth(215);
        bodyImgView.setFitHeight(340);
        
        // Clothing navigation buttons
        Button prevShirt = new Button("←");
        Button nextShirt = new Button("→");
        Button prevPants = new Button("←");
        Button nextPants = new Button("→");
        Button prevHat = new Button("←");
        Button nextHat = new Button("→");

        Button done1 = new Button("Done1");
        Button done2 = new Button("Done2");
        Button done3 = new Button("Done3");
        done1.setOnAction(en -> {
            avatar1.setBodyType(currentAvatar.getBodyType().toString()); // Save the body type
    avatar1.setName(currentAvatar.getName()); // Save the name
    // Optionally save any clothing changes here
    avatar1.setClothingOptions("shirt", new String[]{currentAvatar.getClothingImg("shirt")});
    avatar1.setClothingOptions("pants", new String[]{currentAvatar.getClothingImg("pants")});

    // Switch back to the closet scene (or previous scene)
    primaryStage.setScene(closet);
                });
        done2.setOnAction(en -> {
            avatar2.setBodyType(currentAvatar.getBodyType().toString()); // Save the body type
    avatar2.setName(currentAvatar.getName()); // Save the name
    // Optionally save any clothing changes here
    avatar2.setClothingOptions("shirt", new String[]{currentAvatar.getClothingImg("shirt")});
    avatar2.setClothingOptions("pants", new String[]{currentAvatar.getClothingImg("pants")});
    avatar2.setClothingOptions("hat", new String[]{currentAvatar.getClothingImg("hat")});

    // Switch back to the closet scene
    primaryStage.setScene(closet);
                });
        done3.setOnAction(en -> primaryStage.setScene(closet));

        
        
        // Method to set up the avatar selection
            if (e.getSource() == editAvatar1) {
                currentAvatar = avatar1;
            } else if (e.getSource() == editAvatar2) {
                currentAvatar = avatar2;
            } else if (e.getSource() == editAvatar3) {
                currentAvatar = avatar3;
            }
            
            bodyImgView.setImage(currentAvatar.getBodyType());
            //avatarLabel.setText(currentAvatar.getClothingText()); // Update label
            shirtImg.setImage(new Image(currentAvatar.getClothingImg("shirt")));
            pantImg.setImage(new Image(currentAvatar.getClothingImg("pants")));
            hatImg.setImage(new Image(currentAvatar.getClothingImg("hat")));

            
            shirtImg.setImage(new Image(currentAvatar.getClothingImg("shirt")));
            pantImg.setImage(new Image(currentAvatar.getClothingImg("pants")));
            hatImg.setImage(new Image(currentAvatar.getClothingImg("hat")));
            
            
            //new
            genMale.setOnMouseClicked(en -> {
            currentAvatar = new Boy("New Boy");
        bodyImgView.setImage(currentAvatar.getBodyType());
        shirtImg.setImage(new Image(currentAvatar.getClothingImg("shirt")));
        pantImg.setImage(new Image(currentAvatar.getClothingImg("pants")));
        hatImg.setImage(new Image(currentAvatar.getClothingImg("hat")));
        System.out.println("Male selected");
            });
            genfemale.setOnMouseClicked(en -> {
            currentAvatar = new Girl("New Girl");
        bodyImgView.setImage(currentAvatar.getBodyType());
        shirtImg.setImage(new Image(currentAvatar.getClothingImg("shirt")));
        pantImg.setImage(new Image(currentAvatar.getClothingImg("pants")));
        hatImg.setImage(new Image(currentAvatar.getClothingImg("hat")));
        System.out.println("Female selected");
            });
            genNeut.setOnMouseClicked(en -> {
            currentAvatar = new Neutral("New Neutral");
        bodyImgView.setImage(currentAvatar.getBodyType());
        shirtImg.setImage(new Image(currentAvatar.getClothingImg("shirt")));
        pantImg.setImage(new Image(currentAvatar.getClothingImg("pants")));
        hatImg.setImage(new Image(currentAvatar.getClothingImg("hat")));
        System.out.println("Neutral selected");
            });
            
            light.setOnMouseClicked(en -> {
            if (currentAvatar != null) {
                currentAvatar.setBodyType("/pics/male.png");
                System.out.println("Light skin selected");
            }
            });
            med.setOnMouseClicked(en -> {
            if (currentAvatar != null) {
                currentAvatar.setBodyType("/pics/girlBody.png");
                System.out.println("Medium skin selected");
            }
            });
            dark.setOnMouseClicked(en -> {
            if (currentAvatar != null) {
                currentAvatar.setBodyType("/pics/dark.png");
                System.out.println("Dark skin selected");
            }
            });
            //end new
            
            // Now set clothing button actions
            prevShirt.setOnAction(ev -> { 
                currentAvatar.previousClothing("shirt"); 
                shirtImg.setImage(new Image(currentAvatar.getClothingImg("shirt")));
            });
            nextShirt.setOnAction(ev -> { 
                currentAvatar.nextClothing("shirt"); 
                shirtImg.setImage(new Image(currentAvatar.getClothingImg("shirt")));
            });

            prevPants.setOnAction(ev -> { 
                currentAvatar.previousClothing("pants"); 
                pantImg.setImage(new Image(currentAvatar.getClothingImg("pants")));
            });
            nextPants.setOnAction(ev -> { 
                currentAvatar.nextClothing("pants"); 
                pantImg.setImage(new Image(currentAvatar.getClothingImg("pants")));
            });

            prevHat.setOnAction(ev -> { 
                currentAvatar.previousClothing("hat"); 
                hatImg.setImage(new Image(currentAvatar.getClothingImg("hat")));
            });
            nextHat.setOnAction(ev -> { 
                currentAvatar.nextClothing("hat"); 
                hatImg.setImage(new Image(currentAvatar.getClothingImg("hat")));
            });
            
            VBox avatarEditor = new VBox(30);
                avatarEditor.setAlignment(Pos.CENTER);
                avatarEditor.getChildren().addAll(
                avatarLabel,
                new HBox(20,
                new VBox(60, prevHat, prevShirt, prevPants){{setAlignment(Pos.CENTER);}},
                new StackPane(bodyImgView, pantImg, shirtImg, hatImg), 
                new VBox(60, nextHat, nextShirt, nextPants){{setAlignment(Pos.CENTER);}}){{setAlignment(Pos.CENTER);}},
                genderLayout, skinLayout
        );

        if (currentAvatar == avatar1){
            avatarEditor.getChildren().add(done1);
        } else if (currentAvatar == avatar2){
            avatarEditor.getChildren().add(done2);

        } else{
            avatarEditor.getChildren().add(done3);

        }
        
        Scene editor = new Scene(avatarEditor, 500, 500);

            primaryStage.setScene(editor);
        };

        // Assign event handlers to avatar buttons
        editAvatar1.setOnAction(avatarSelectionHandler);
        editAvatar2.setOnAction(avatarSelectionHandler);
        editAvatar3.setOnAction(avatarSelectionHandler);
                
                
                
        primaryStage.setTitle("Dress Up Game!");
        primaryStage.setScene(menuScene);
        primaryStage.show();
    }

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        launch(args);
    }
    
}



Avatar:

public class Avatar {
    //protected so girl boy can access
    protected HashMap<String, String[]> clothingOptions;
    private HashMap<String, Integer> clothingIndex;
    private String name;
    private Image bodyType;

    public Avatar(String name) {
        this.name = name;
        clothingOptions = new HashMap<>();
        clothingIndex = new HashMap<>();

        // Clothing options
        clothingOptions.put("shirt", new String[]{"/pics/Gtop1.png"});
        clothingOptions.put("pants", new String[]{"/pics/Gtop1.png"});
        clothingOptions.put("hat", new String[]{"/pics/Gtop1.png"});

        // Default to first clothing item
        clothingIndex.put("shirt", 0);
        clothingIndex.put("pants", 0);
        clothingIndex.put("hat", 0);
        
        setBodyType("/pics/male.png");
    }

    public void nextClothing(String type) {
        int currentIndex = clothingIndex.get(type);
        int newIndex = (currentIndex + 1) % clothingOptions.get(type).length;
        clothingIndex.put(type, newIndex);
    }

    public void previousClothing(String type) {
        int currentIndex = clothingIndex.get(type);
        int newIndex = (currentIndex - 1 + clothingOptions.get(type).length) % clothingOptions.get(type).length;
        clothingIndex.put(type, newIndex);
    }

    public String getClothingImg(String type) {
        return clothingOptions.get(type)[clothingIndex.get(type)];
    }

    public String getName() {
        return name;
    }
    public void setName(String name){
        this.name = name;
    }
    
    public Image getBodyType(){
        return bodyType;
    }
    
    public Image setBodyType(String imagePath){
        this.bodyType = new Image(imagePath);
        return bodyType;
    }
    public void setClothingOptions(String clothingType, String[] options) {
        clothingOptions.put(clothingType, options);
    }
}
